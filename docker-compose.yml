services:
  mysql:
    image: mysql:8.0
    container_name: kalshi_mysql
    command:
      - --performance-schema=OFF
      - --innodb-buffer-pool-size=64M
      - --max-connections=25
      - --table-open-cache=200
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: ${MYSQL_DATABASE:-signal_logger}
      MYSQL_USER: ${MYSQL_USER:-logger_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-logger_password}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    ports:
      - "127.0.0.1:3306:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot_password"]
      interval: 10s
      timeout: 5s
      retries: 30
      start_period: 120s
    restart: unless-stopped

  logger:
    build: .
    container_name: kalshi_logger
    env_file:
      - .env
    volumes:
      - ./kalshi-key.pem:/app/kalshi-key.pem:ro
    depends_on:
      mysql:
        condition: service_healthy
    restart: unless-stopped

  api:
    build: .
    container_name: kalshi_api
    command: ["uvicorn", "app.api:app", "--host", "0.0.0.0", "--port", "8000"]
    env_file:
      - .env
    volumes:
      - ./kalshi-key.pem:/app/kalshi-key.pem:ro
    depends_on:
      mysql:
        condition: service_healthy
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped

volumes:
  mysql_data:
